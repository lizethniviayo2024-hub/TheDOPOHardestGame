package domain;

/**
 * Interfaz que define el comportamiento de movimiento.
 * Cualquier tipo de movimiento (horizontal, vertical, zigzag) la implementa.
 */
public interface Movement {
    void move();
}
